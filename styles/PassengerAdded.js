import { StyleSheet } from 'react-native';

const PassengerAddedStyles = StyleSheet.create({
  Container: {
    flex: 1,
    alignContent: 'center',
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 50,
    marginBottom: -20,
  },
});

export default PassengerAddedStyles;
